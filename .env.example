# Copy to ".env" and fill in from the Intuit Developer portal (Keys & OAuth).
QBO_CLIENT_ID=your_client_id
QBO_CLIENT_SECRET=your_client_secret

# Must EXACTLY match a Redirect URI registered on your app.
QBO_REDIRECT_URI=http://localhost:8000/callback

# "sandbox" to test against Intuit's demo company, "production" for your real books.
QBO_ENVIRONMENT=sandbox

# Optional for localhost only. A hosted, HTTPS, or production deployment refuses
# to start without this password.
# APP_PASSWORD=pick-a-good-password

# Optional two-factor. If set, login also requires the current 6-digit code
# from an authenticator app. Generate the value by visiting /mfa-setup while
# signed in. Leave unset for password-only.
# TOTP_SECRET=
